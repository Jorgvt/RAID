use `uv` to manage this project. scripts can be run with `uv run <script.py>`. packages can be added with `uv add <package>`.
keep a our advances and discussions documented in appropriate files. Keep them separate and tidy. Avoid having a monolithic doc file.
ask questions and clarifications when needed.
this is a scientific project so keeping good rigor and grounding our claims in references is important.
